<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tianxi.getphonenumber" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Hack007_01Activity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity2Activity"
            android:label="@string/title_activity_main_activity2" >
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_hack001" >
        </activity>
        <activity
            android:name=".Hack001Activity"
            android:label="@string/title_activity_hack004" >
        </activity>
        <!-- <activity -->
        <!-- android:name=".Hack005Activity" -->
        <!-- android:label="@string/title_activity_hack005" > -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".Hack006Activity" -->
        <!-- android:label="@string/title_activity_hack006" > -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".Hack007Activity" -->
        <!-- android:label="@string/title_activity_hack007" > -->
        <!-- </activity> -->
        <activity
            android:name=".Hack007Activity"
            android:label="@string/title_activity_hack007_01" >
        </activity>
    </application>

</manifest>
